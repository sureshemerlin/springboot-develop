package com.sample
final person = library('person')

class Person {
    def checkOutFrom(input) {
        print "Hello World  ${input}"
    }

    String takeString(String message) {
        return " Returned from takestring............ ${message}"
    }
}